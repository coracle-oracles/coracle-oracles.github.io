This is a django web framework-based project, using `uv` for package management. Environment variable secrets are stored in `.env`.

If you make changes to the schema, update `seed.sql` if necessary.
